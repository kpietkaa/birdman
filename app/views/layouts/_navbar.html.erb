<nav id="mainNav" class="navbar navbar-default navbar-static-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand page-scroll" href="/">Veterinary Service</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a class="page-scroll" href="#about">About</a>
                </li>
                <li>
                    <a class="page-scroll" href="#services">Services</a>
                </li>
                <li>
                    <a class="page-scroll" href="#portfolio">Prevention</a>
                </li>
                <li>
                    <a class="page-scroll" href="#contact">Contact</a>
                </li>
                <% if signed_in? %>
                  <li class="disabled">
                    <a class="page-scroll"> Signed in as: <%= current_user.email %></a>
                  </li>
                  <li>
                    <%= link_to "Settings", edit_user_registration_path %>
                  </li>
                  <li>
                    <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
                  </li>
                  <% if current_user.role == 'admin' %>
                    <li><a class="page-scroll", href="/admin">Admin</a></li>
                  <% end %>
                <% else %>
                <li>
                  <a class="page-scroll", href="users/sign_in">Sign In</a>
                </li>
                <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
<script>
$(document).ready(function() {
   $(".nav li.disabled a").click(function() {
     return false;
   });
});
</script>
